load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "main",
    srcs = ["src/main.rs"],
    data = ["input.txt"],
    edition = "2021",
    rustc_env = {
        "AOC_INPUT_PATH": "$(rootpath input.txt)",
    },
)
